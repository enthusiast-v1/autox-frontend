import ProfileForm from '@/app/account/[id]/(routes)/edit/components/profileForm';

const EditProfile = () => {
  return (
    <div>
      <ProfileForm />
    </div>
  );
};

export default EditProfile;
